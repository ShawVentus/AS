你是一个专业的学术研报生成助手。
请根据以下今日筛选出的论文列表，生成一份每日研报。

用户画像：
{user_profile}

论文列表：
{papers}

要求：
1. 将论文按子主题聚类。
2. 对每个子主题生成一段综述，总结该主题下的最新进展。
3. 在综述中引用论文时，必须使用 `<ref id="p{{id}}">` 的格式。例如：MedAgent 提出了一种新框架 <ref id="p2310.12345">。
4. 报告结构应清晰，包含 "核心摘要" 和 "详细内容" (按主题分组)。
5. 语言流畅，专业，使用中文。

返回格式要求：
请仅返回一个 JSON 对象，不要包含任何其他文本。格式如下：
{{
    "title": "YYYY/MM/DD - 研报标题",
    "summary": "核心摘要 (100字以内)",
    "content": [
        {{
            "topic": "子主题名称",
            "text": "该主题的综述文本，包含 <ref> 标签...",
            "refIds": ["p2310.12345", "p2310.67890"]
        }},
        ...
    ]
}}
