name: Daily ArxivScout Report

on:
  schedule:
    - cron: "0 3 * * *" # UTC 07:00 = 北京时间 11:00
  workflow_dispatch: # 支持手动触发

concurrency:
  group: daily-report
  cancel-in-progress: false

jobs:
  daily-report:
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          ref: "0.1"

      - name: 设置 Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # ===== pip 缓存加速 =====
      - name: 缓存 pip 依赖
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('backend/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: 安装依赖
        run: |
          cd backend
          pip install -r requirements.txt

      - name: 执行每日工作流
        env:
          # 数据库配置
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          # LLM 配置
          BOHRIUM_API_KEY: ${{ secrets.BOHRIUM_API_KEY }}
          LLM_PROVIDER: ${{ secrets.LLM_PROVIDER }}
          MODEL_CHEAP: ${{ secrets.MODEL_CHEAP }}
          MODEL_PERFORMANCE: ${{ secrets.MODEL_PERFORMANCE }}
          LLM_ANALYSIS_BATCH_SIZE: ${{ secrets.LLM_ANALYSIS_BATCH_SIZE }}
          LLM_ANALYSIS_BATCH_DELAY: ${{ secrets.LLM_ANALYSIS_BATCH_DELAY }}
          LLM_MAX_WORKERS: ${{ secrets.LLM_MAX_WORKERS }}
          # 邮件配置
          SMTP_SERVER: ${{ secrets.SMTP_SERVER }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SENDER_EMAIL: ${{ secrets.SENDER_EMAIL }}
          SENDER_PASSWORD: ${{ secrets.SENDER_PASSWORD }}
          ADMIN_EMAILS: ${{ secrets.ADMIN_EMAILS }}
          # ArXiv 爬虫配置
          ARXIV_DELAY_SECONDS: ${{ secrets.ARXIV_DELAY_SECONDS }}
          ARXIV_NUM_RETRIES: ${{ secrets.ARXIV_NUM_RETRIES }}
          # 报告配置
          REPORT_PAPER_LIMIT: ${{ secrets.REPORT_PAPER_LIMIT }}
          # 工作流配置
          WORKFLOW_MAX_RETRIES: ${{ secrets.WORKFLOW_MAX_RETRIES }}
          WORKFLOW_RETRY_DELAY_BASE: ${{ secrets.WORKFLOW_RETRY_DELAY_BASE }}
        run: |
          cd backend
          python3 -c "from app.services.scheduler import scheduler_service; scheduler_service.run_daily_workflow(force=True)"

      # ===== 日志文件上传 =====
      - name: 上传执行日志
        uses: actions/upload-artifact@v4
        if: always() # 无论成功失败都上传
        with:
          name: workflow-logs-${{ github.run_id }}
          path: backend/logs/
          retention-days: 30 # 保留 30 天
